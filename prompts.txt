Can you please create a new directory now 'Rust', and guide me step by step through writing my own implementation?

Start with the high level abstractions then move down -- at each step just let me know what the goal is (e.g. 'make a linked list for X and randomize the initial values, make a map for Y, etc.') and we can see if I can do it 


The goal for the neural network would be to:

Define a structure for the neural network
Implement forward pass
Implement weight initialization
Ensure it matches the C++/Python architecture [8, 16, 4, 1]
Would you like to start with creating the neural network module? We can break it down into smaller steps if you prefer.

The key differences we'll leverage in Rust:

Zero-cost abstractions
Better memory management
Better parallelization capabilities
Better type safety
Better performance out of the box


The goal for the neural network would be to:

Define a structure for the neural network
Implement forward pass
Implement weight initialization
Ensure it matches the C++/Python architecture [8, 16, 4, 1]
Would you like to start with creating the neural network module? We can break it down into smaller steps if you prefer.

The key differences we'll leverage in Rust:

Zero-cost abstractions
Better memory management
Better parallelization capabilities
Better type safety
Better performance out of the box